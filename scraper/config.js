// Multi-file constants go here, though the goal is to avoid needing
// this file.

const BOX_SCORE_REGEX = /Box-([0-9]*)-([0-9]*)\.htm$/;
//const id = Number(match[2]);

module.exports = {
  BOX_SCORE_REGEX
};
